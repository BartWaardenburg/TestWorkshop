import { generateFancyObject, generateArray } from './snapshot';

/**
 * 2.1 Create a snapshot
 */
test('The generateFancyObject should generate a fancy object', () => {
	// expect(...).toMatchSnapshot();
});

/**
 * 2.2 Create a snapshot for the generateArray function
 */
