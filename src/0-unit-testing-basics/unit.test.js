import { add, multiply, packages, addPackage, addPackagePure } from './unit';

/**
 * 0.1 Assertions
 */
test('Add should return the combined value of its two parameters', () => {
	// expect(add(...)).toBe(...);
});

test('Multiply should return the multiplication of its two parameters', () => {
	// expect(multiply(...)).toBe(...);
});

/**
 * 0.2 Assertions with side effects
 */
test('The addPackage function should add a package to the packages collection', () => {
	// expect(...).toContain(...);
});

test('The addPackagePure function should add a package to the packages collection', () => {
	// expect(...).toContain(...);
});

test('The addPackagePure function should only add one package to the collection', () => {
	// expect(...).toHaveLength(...);
});

/**
 * 0.3 BONUS Think of other ways to make sure the package function
 * does what it should be doing
 */
